{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Run single test",
      "program": "${workspaceFolder}/${relativeFile}",
      "cwd": "${workspaceFolder}",
      "runtimeExecutable": "${workspaceFolder}/node"
    }
  ]
}
